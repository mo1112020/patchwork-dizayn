/** Designer defaults (no color picker – textures only). */

export const PRICE_PER_SQM = 150;
export const DEFAULT_RUG_WIDTH = 2;
export const DEFAULT_RUG_HEIGHT = 3;
/** 5 cm grid so e.g. 90 cm width = 18 cells → 2 equal columns of 45 cm (9+9). */
export const GRID_UNIT_SIZE = 0.05;
export const CANVAS_GRID_SIZE = 40;
export const DEFAULT_WASTE_ALLOWANCE = 5;
export const DEFAULT_PRECISION_TOLERANCE = 2;
