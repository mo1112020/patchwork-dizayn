services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: always

# Optional: Add Supabase local dev if needed, 
# but usually users just want to containerize the frontend
